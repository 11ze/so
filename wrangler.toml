# Cloudflare Workers 配置文件
# 文档: https://developers.cloudflare.com/workers/wrangler/configuration/

name = "so"
main = "src/worker.js"
compatibility_date = "2024-01-01"

# 文本模块导入：模板/样式/客户端脚本以文本内联进单文件页面（零构建，见 docs/adr/0001）
[[rules]]
type = "Text"
globs = ["**/template.html", "**/styles.css", "**/client.js"]
fallthrough = true

# 本地开发配置
[env.dev]
name = "so-dev"

# 环境变量（如果需要）
# vars = { ENVIRONMENT = "development" }

# 如果需要使用 KV/D1/R2 等绑定，在这里配置
# [[kv_namespaces]]
# binding = "KV_NAMESPACE"
# id = "xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"

# 如果需要使用 D1 数据库
# [[d1_databases]]
# binding = "DB"
# database_name = "so-search-db"
# database_id = "xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"
